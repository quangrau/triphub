// import { types } from './actions';

const initialState = {
  signedIn: false,
  token: null
};

const session = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default session;
